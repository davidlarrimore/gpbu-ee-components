<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contact__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>REQUIRED!!! This links the demo setup record to an Employee (Contact), which is heavily leveraged for every demo scenario.</description>
    <externalId>false</externalId>
    <inlineHelpText>REQUIRED!!! This links the demo setup record to an Employee (Contact), which is heavily leveraged for every demo scenario.</inlineHelpText>
    <label>Contact</label>
    <referenceTo>Contact</referenceTo>
    <relationshipLabel>Demo Setup (Contact)</relationshipLabel>
    <relationshipName>Demo_Setup</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
